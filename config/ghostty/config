font-family = Dank Mono
# font-family = BlexMono Nerd Font Mono
# font-family = JetBrainsMono Nerd Font Mono
# font-family = Comic Code Ligatures   # kept for machines that have Comic Code installed
font-size = 14

# Cursor: hacker green #00FF00, non-blinking. (Was teal #1abc9c; kitty/wezterm
# still use teal.) The cursor-trail shader below inherits this color.
cursor-color = #00FF00
cursor-text = #161819
cursor-style = block
cursor-style-blink = false

# Animated cursor effect via a custom GLSL shader (Ghostty has no built-in one
# as of 1.3.x). Pick ONE effect by uncommenting its custom-shader line below.
# IMPORTANT: Ghostty config does NOT support trailing/inline comments — a value
# runs to the end of its line — so the active custom-shader line must be the bare
# path with NO "# ..." after it, or the shader silently fails to load. Per-shader
# tunables (DURATION, sizes, colors) live at the top of each .glsl file.
#   custom-shader-animation = true animates only on activity (cursor moves), so
#   the GPU idles when nothing's happening — this is the power-saving choice and
#   is plenty for cursor_warp (it only needs to react to cursor movement).
#   Switch to `always` to keep effects animating even when the window is unfocused
#   or idle — needed only for self-animating shaders, at the cost of the GPU
#   running continuously (measurably warmer laptop, e.g. while watching video).
# Paths are relative to this config file. A custom-shader change needs a full
# Ghostty relaunch (cmd+shift+, reload does not reliably re-load shaders).
#
# Effect legend (uncomment exactly one line in the block below):
#   cursor_tail.glsl          — subtle kitty-style comet trail
#   cursor_blaze.glsl         — fiery yellow→red smear on jumps
#   sonic_boom_cursor.glsl    — green shockwave on cursor-shape change (vim modes)
#   cursor_smear.glsl         — solid green hexagonal smear on movement (active)
#   cursor_smear_rainbow.glsl — rainbow plasma smear on movement
#   cursor_warp.glsl          — smooth Neovide smear, long tapered green trail
#
# custom-shader = shaders/cursor_tail.glsl
# custom-shader = shaders/cursor_blaze.glsl
# custom-shader = shaders/sonic_boom_cursor.glsl
custom-shader = shaders/cursor_smear.glsl
# custom-shader = shaders/cursor_smear_rainbow.glsl
# custom-shader = shaders/cursor_warp.glsl
custom-shader-animation = true

# Colorscheme is switchable at runtime with the `theme` command
# (gruvbox <-> tokyo-night), which repoints theme-active.conf. The theme name,
# background, and split colors live in the theme-{gruvbox,tokyo-night}.conf
# fragments; this include applies whichever one is active. Ghostty has no signal
# reload, so existing windows need a manual reload (cmd+shift+,); new windows
# pick it up automatically.
config-file = theme-active.conf

# Treat the macOS Option key as a real Alt modifier (instead of composing
# special characters like ¡™£), so Alt-chord shortcuts reach terminal apps —
# e.g. herdr's prefix+alt+1..9 workspace jumps. Use "left" instead if you want
# to keep the right Option key for composing accented characters.
macos-option-as-alt = true

# Window background opacity. The command palette action
# "Toggle Background Opacity" switches between this value and fully opaque.
background-opacity = 0.50

# Keep explicitly painted background cells opaque (only the default background
# gets the opacity). nvim themes run transparent (no painted bg) and herdr's
# background is "reset", so translucency still shows everywhere it used to —
# but UI highlights like herdr's active tab now render at full color instead
# of being washed to 45% (accent #9ecbff was showing as ~#536882).
background-opacity-cells = false

# Keep background windows sharp when multitasking.
background-blur = false

# Rounded corners, no titlebar/buttons. Keeps the window frame (needed for the
# rounded corners); the "white border" people see is actually the shadow rim,
# killed below with macos-window-shadow = false.
macos-titlebar-style = hidden
# window-decoration = none           # fully borderless but squares the corners
# macos-titlebar-style = transparent

# Drop the macOS shadow — its bright rim shows as a thin light edge on a
# borderless window.
macos-window-shadow = false

# Opacity for unfocused splits (0.0 to 1.0)
unfocused-split-opacity = 0.7

# unfocused-split-fill and split-divider-color are theme-specific colors; they
# live in the theme-*.conf fragments pulled in via config-file above.

# Scrollback buffer per terminal surface, in bytes. Default is 10 MB
# (10000000); bumped to 50 MB so long agent/build output stays scrollable.
# Byte-based, not a fixed line count. Applies to new surfaces (tabs/windows)
# created after a config reload — it does not recover already-dropped lines.
scrollback-limit = 50000000

# Clipboard: make copying land on the macOS system clipboard.
#   clipboard-write = allow lets programs running inside Ghostty (herdr, tmux,
#   Neovim) push to the system clipboard via OSC 52 without a prompt. This is
#   already Ghostty's default, but pinned here so a copy from inside a
#   multiplexer always reaches pbpaste.
#   copy-on-select = clipboard also puts mouse-selected text straight onto the
#   system clipboard (the default only copies to Ghostty's internal selection
#   buffer). Trade-off: every selection overwrites the clipboard — drop this
#   line if that clobbering is unwanted and rely on explicit Cmd-C instead.
# clipboard-read is left at its default (ask) since only copying was requested.
clipboard-write = allow
copy-on-select = clipboard
